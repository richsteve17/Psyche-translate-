# Anthropic API key for roleplay, drill grading, and content generation.
# Prefix with EXPO_PUBLIC_ so it's bundled into the client.
# WARNING: this means the key is embedded in the app binary. Acceptable
# for personal/single-user MVP; rotate and move behind a backend before
# distributing.
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-...
